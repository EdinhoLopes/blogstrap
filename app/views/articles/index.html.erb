<%= render 'longer_featured' %>

<div class="row mb-2">
  <%= render 'medium_featured' %>
  <%= render 'medium_featured' %>
</div>

<div class="row g-5">
  <div class="col-md-8">
    <h3 class="pb-4 mb-4 fst-italic border-bottom">
      From the Firehose
    </h3>

    <p>
      <%= link_to 'New Article', new_article_path, class: 'btn btn-primary' %>
    <p>
    
    <!-- Este código, substitui... -->
    <%= render @articles %>

    <!-- ... este rsrsrs, menos código pra escrever...
    <% @articles.each do |article| %>
      <%= render article %>
    <% end %>
    -->

    <%= render 'pagination' %>
  </div>
  <%= render 'right_side' %>
</div>